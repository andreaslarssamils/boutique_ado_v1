# Local development environment variables
# Copy to .env and fill in values.

# Django
DEVELOPMENT=1

# Stripe (required for /checkout/)
STRIPE_PUBLIC_KEY=
STRIPE_SECRET_KEY=
STRIPE_WH_SECRET=

# Optional email vars (only used when DEVELOPMENT is not set)
EMAIL_HOST_USER=
EMAIL_HOST_PASS=
